import sys #unknown import
import os
os.add_dll_directory(r"C:\Users\eliot\AppData\Local\LAMMPS USER-BFIELD Plugin 4Jul2026-MSMPI")
try: #try this and if it works then 
    from lammps import lammps
    print("Python module 'lammps' imported successfully!")
    
    # Try to initialize the LAMMPS engine
    lmp = lammps() #initailizer
    print(f"LAMMPS Version: {lmp.version()}") #fstrings
    lmp.command("print 'Everything is working perfectly!'")
    
except ImportError:
    print("ERROR: The 'lammps' module is not installed in this Python environment.")
    print(f"Current Python executable: {sys.executable}")
except Exception as e:
    print(f"Import succeeded, but LAMMPS engine failed to start: {e}")